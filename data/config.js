/* ============ Configuration de la synchronisation en ligne ============
   Supabase (gratuit) : connexion par e-mail + sauvegarde de la progression.
   Laisse ces deux valeurs vides pour une utilisation 100 % locale
   (la progression reste alors dans le navigateur).                        */
const CONFIG = {
  SUPABASE_URL: '',         // ex : 'https://xxxx.supabase.co'
  SUPABASE_ANON_KEY: '',    // clé publique « anon » (sans danger, protégée par RLS)
};